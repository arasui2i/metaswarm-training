import { Typography, Box } from '@mui/material';

export function ForgotPasswordPage() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4">Forgot Password</Typography>
      <Typography color="text.secondary">Password reset coming soon.</Typography>
    </Box>
  );
}
