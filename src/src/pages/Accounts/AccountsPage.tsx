import { Box, Typography } from '@mui/material';

export function AccountsPage() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4">Accounts</Typography>
      <Typography variant="body1" sx={{ mt: 1 }}>Coming soon.</Typography>
    </Box>
  );
}
